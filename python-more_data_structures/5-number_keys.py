#!/usr/bin/python3
def number_keys(a_dictionary):
    """
    Count keys from a dictionary.

    Parameters:
        a_dictionary : the dictionary.

    Returns :
        Length of key list from the dictionary.
    """
    return len(list(a_dictionary))
